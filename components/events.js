import React, { Component } from 'react';

export default (props) => {
  <section>
    <div className="row">
      {props.events.map(event =>
        <div className="card">
          
        </div>
      )}
    </div>
  </section>
}
